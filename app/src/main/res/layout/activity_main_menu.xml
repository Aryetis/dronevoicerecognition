<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main_menu"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.dvr.mel.dronevoicerecognition.MainMenuActivity"

    tools:theme="@style/AppTheme"
    android:background="@color/colorAccent">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/menuLinearLayout"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true">

        <Button
            android:text="Manage Corpora"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/manageCorporaButton"
            android:background="@color/colorPrimary"
            android:layout_margin="10dp"
            android:onClick="startManageCorporaActivity"
            android:textColor="@color/colorAccent" />

        <Button
            android:text="Test Corpus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/testCorpusButton"
            android:background="@color/colorPrimary"
            android:layout_margin="10dp"
            android:textColor="@color/colorAccent" />

        <Button
            android:text="Mic Calibration"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/micCalibrationButton"
            android:background="@color/colorPrimary"
            android:onClick="startMicCalibrationActivity"
            android:layout_margin="10dp"
            android:textColor="@color/colorAccent"/>

    </LinearLayout>
</RelativeLayout>
